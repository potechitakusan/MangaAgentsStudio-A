# 漫画制作キットの作業ルール

- NovelAIで有効なOpus契約を使う場合、１回１枚、28ステップ以下（V5は初期値23、V4.5以前は28）を既定とする。生成寸法は使用ツールに合わせる共通のページ設定に従い、生成実寸に枠と最終PNGを合わせる。採用寸法を含む全設定、V5の利用上限・参照等の追加料金を実行前に確認し、有料利用の明示指示なしにAnlasを消費する生成へ進まない。詳細・出典は `docs/knowledge/ai-production.md` のOpus節。他の生成環境には一律適用しない。

- 基本は「キャラ・あらすじ・ページ数を指定すると、既定動作でインターネット公開用の漫画PNGを制作できる」構成にする。用途・好みの違いは作品の `OPTION.md` で選べるようにし、印刷専用の設定は `PRINT-OPTION.md` へ分ける。追加設定への一括回答を制作開始条件にしない。
- 新規作品の寸法オプションは「生成時の希望／生成後の実寸 → 枠配置 → 最終PNG」を区別して幅×高さで案内する。既定は１ページ全体の生成画像の実寸に枠を合わせ、同じ寸法で書き出す。余白・線幅も連動して換算する。同梱枠素材の2000×3000pxを生成・完成画像の指定として案内しない。詳細は `docs/knowledge/page-layout.md`。
- セリフの文字組みは原則上詰め。セリフ・ナレーションに「。」は使わない。「、」は１個の吹き出し・ナレーション枠の本文が20文字以上で、長文を読みやすく区切るために必要な場合だけ使う。つながった枠は１個として合算する。文字数の数え方・配置・点検は `docs/knowledge/japanese-manga-readability.md` に従う。
- 日本語推敲スキルは既定で無効。有効にした作品では最初の作業時に自動導入する。Gitは導入済みを前提とし、なければインストールしてよいか確認する。Geminiを前提とするスキルは、利用環境があっても導入前に文章の送信・利用枠の消費許可を必ず得る。選択だけで許可とみなさない。詳細は `docs/knowledge/optional-skills.md`。取得物・承諾記録をキットや新規作品へ同梱しない。

- このフォルダは漫画制作キットの保守・配布用。原則として、このルートや配下のフォルダ（`templates/manga-project/` を含む）では漫画を制作しない。制作依頼を受けたら `scripts/New-MangaProject.ps1` を実行し、キット外に作品ごとのプロジェクトを作る。既定の作成先は `../作品名/`。既存作品の続きはその作品フォルダを使う。キットの文書・スクリプトの修正や `.work/` 内の配布テストはここで行える。
- 作成成功と作成先の実在を確認し、ユーザーへの完了メッセージに作品プロジェクトの絶対パスを省略せず示して、「このフォルダでCodexを開き直してください」と案内する。「やり方がわからない場合は、ご利用の環境（Windows／Mac、アプリ／VS Code）を教えてください。必要な手順をご案内します」と添え、作成のたびに操作手順の調査・説明は行わない。ユーザーが手順を求めた場合だけ、利用環境に合う最新の公式情報を検索・閲覧して日本語で説明し、出典URLと確認日を添える。詳細は `docs/knowledge/codex-operation.md`。作成だけで現在のCodexの作業フォルダが切り替わったと扱わず、作品フォルダで開いたセッションから企画・字コンテ・ネーム・作画を始める。
- ユーザーがどうしてもこのキット内で漫画を作ると明示した場合だけ、例外の手順を案内する。キットのルートに `特別な理由でこのフォルダの中で漫画を作ります.txt` をユーザー自身に作成してもらい、その通常ファイルの存在を確認するまで制作を始めない。Codexが代わりに作成・コピー・生成コマンドの実行をしてはならず、雛形にも含めない。ファイルの存在だけで制作の指示とみなさず、返答なしも作成済みと扱わない。詳しくは `README.md` の例外手順を参照する。
- このフォルダでできることを聞かれたら、まず個別プロジェクトを作って開き直す使い方を説明し、`README.md` の工程図と `docs/knowledge/manga/08-workflow-review.md` を基に作品側でできることを説明する。漫画の企画・字コンテ・ネームを主軸にし、映画・動画は必要時の補助にする。
- 漫画はPNGプレビューを提示し、修正を反映・再提示して、修正がないことを確認する。PSD作成の指示がなければ作るか聞いて回答を待つ。既に指示があれば再確認せず、PNGの修正確認後に作成・検証・引き渡しへ進む。返答なしを確認済みと扱わない。詳細は `docs/knowledge/psd-handoff.md`。
- 原則、人間がCLIP STUDIO PAINTで仕上げて外部公開する。CBZ作成と動画試作は明示依頼がある場合だけ行う。
- 説明、ノウハウ、記入項目、引き継ぎ、レイヤー名は日本語にする。製品名、出典URL、パス、機械用識別子、ライセンス原文は維持する。
- 文書・設定・保存記録の作業パスは相対パスにする。開き直し先をユーザーへ案内するメッセージには、実在を確認した絶対パスを表示する。リポジトリのフォルダ名を固定しない。外部URL、ツール内部のパス解決、ユーザーへの一時的な表示と保存記録を区別する。
- 制作の共通知識・運用は `docs/knowledge/`。人物・情報・演技は `docs/knowledge/manga/`、読みやすさと共通レビューはknowledge直下の該当文書へ統合し、用途が同じ内容は該当文書へ集約する。参照先と利用Skillの対応は `docs/knowledge/review-workflow.md`。監修知識は `docs/knowledge/supervision/`。版0.3.2は既存のギャグと、場面監修15テーマの試行版を収録する。必要なテーマだけ読み、資料の主張・制作提案・未検証の効果を区別する。スキル定義は `skills/`、作品雛形は `templates/manga-project/`。監修知識の配布対象は `docs/knowledge/supervision/distribution.json` で限定する。構成と配布は `docs/architecture.md` を参照する。
- 再開時はローカルの `docs/PROGRESS.md` と `docs/PLAN.md` を読む。なければ必要時に作成し、節目に現在地と次の行動を更新する。これらの作業記録、`backup/`、`.work/`、素材・生成物は公開対象外。
- 出典URL・取得日・適用範囲を残し、確認事実、制作提案、未検証事項を区別する。取得日が不明な既存資料は2026-09-12を補完日と明記する。
- 作品側のフィードバックは記録を依頼された場合だけ残す。記録するかの確認、毎回の候補づくり、依頼のない還元は行わない。通常の進捗・修正・検証記録は必要な範囲で残す。
- メインエージェントが制作判断を統合する。スキルは役割別の手順として使う。認証情報、作品素材、未整理ログを共通テンプレートに含めない。
- 公開前のテスト・自動検査は、修正のたびに実行・再実行しない。ユーザーからCommit前のチェックを依頼されたときに、変更内容・過去の結果を踏まえて実施対象と範囲を提示し、実行してよいか確認してから行う。チェックの依頼だけ、返答なし、以前の別範囲への承諾を今回の実行許可とみなさない。修正後の再実行も確認した範囲に限り、範囲外なら理由と対象を示して確認する。既に今回の対象・範囲・再実行について明示許可があれば再質問しない。通常の編集内容・差分の読み取り確認は行い、テスト未実施を合格扱いにしない。詳細は `docs/publication.md`。
- 公開はこのフォルダから直接Gitへコミットする運用とし、公開用ZIPや別の公開用フォルダは作らない。公開準備では `docs/publication.md` の実行確認の手順に従って作業ツリーの公開対象とGitの追加候補・ステージ済み差分を検証する。`examples/` はユーザーが公開対象に指定した作例の置き場で、PNGのExif・テキスト・生成来歴などのメタ情報を除去してから公開対象にする。新規作品の雛形にはコピーしない。コミットや外部への公開・送信は、その操作を依頼された範囲で行う。
